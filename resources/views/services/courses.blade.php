<div>
    <p class="mb-4 px-8 font-semibold text-2xl">{{ __( "I corsi" ) }}</p>

    <div>
        <div class="py-16 px-8">
            <p class="mb-2 font-medium text-xl">{{ __( "I corsi online" ) }}</p>
            <p class="font-thin text-sm">{{ __( "Accessibili a chiunque" ) }}</p>
            <p>{{ __( "Coming soon.." ) }}</p>
        </div>

        <div class="py-16 px-8 bg-agp-yellow">
            <p class="mb-2 font-medium text-xl">{{ __( "I corsi in sede" ) }}</p>
            <p class="font-thin text-sm">{{ __( "Dai un boost al tuo team di lavoro" ) }}</p>
            <p>{{ __( "Scopri il mondo delle nuove tecnologie di sviluppo per il web ed aumenta il valore del tuo team di sviluppo." ) }}</p>
        </div>

        <div class="py-16 px-8">
            <div>
                <p class="mb-2 font-medium text-xl">{{ __( "Un percorso a misura del tuo organico" ) }}</p>
                <p class="font-thin text-sm">{{ __( "Nuovi metodi di sviluppo" ) }}</p>
                <p class="mb-4">{{ __( "Un percorso ideato per ogni ambiente di lavoro che ti permette di colmare il gap tecnologico necessario per l'acquisizione delle competenze che il mercato dello sviluppo online richiede." ) }}</p>
                <p class="p-2 italic">{{ __( "Impara il significato dei nuovi termini abbinati allo sviluppo delle applicazioni web, impara il versioning con semver e git, sfrutta strumenti di gestione delle dipendenze come composer e npm, acquisisci le competenze per lo sviluppo di applicazioni performanti con laravel e le logiche di deploy in un ambiente CD/CI." ) }}</p>
            </div>

            <div class="relative text-center my-16 lg:-mx-4">
                <figure class="max-w-3xl font-display">
                    <blockquote class="text-black relative text-2xl mx-auto italic mb-4">
                        <p>{{ __( "L'ordine per un programmatore è importante quanto la conoscenza, la semplice scrittura del codice porta lontano dal risultato. Andrea sa insegnare anche questo; oltre a conoscere a fondo gli argomenti, sa insegnare il mestiere." ) }}</p>
                        <p class="text-xs font-thin">{{ __( "Net-Evolution" ) }}</p>
                    </blockquote>
                    <figcaption class="flex items-center justify-center">
                        <img src="" alt="" class="rounded-full block h-8 w-8 mr-4" />
                        <a href="https://www.net-evolution.com" class="no-underline text-indigo-dark hover:text-indigo uppercase tracking-wide font-bold text-base">{{ __( "Cristiano Cattaneo" ) }}</a>
                    </figcaption>
                </figure>
            </div>
        </div>
    </div>
</div>
