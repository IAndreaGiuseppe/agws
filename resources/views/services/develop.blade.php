<div class="py-16 px-8">
    <p class="mb-2 font-medium text-xl uppercase">{{ __( "Sviluppo" ) }} &#128190;</p>

    <div class="mb-4">
        <p class="mb-2 font-medium text-xl">{{ __( "Gli standard" ) }}</p>
        <p class="font-thin text-sm">{{ __( "Web standards" ) }}</p>
        <p class="mb-4">{{ __( "La struttura del World Wide Web è definita e descritta in ogni suo aspetto tramite delle specifiche tecniche denominate" ) }} <a href="https://www.w3.org/standards/">{{ __( "Web standards" ) }}</a> {{ __( "o semplicemente Standards." ) }}</p>
        <p class="p-2 italic">{{ __( "Il mio impegno nello studio e applicazione di questi standard sono il punto di forza che mi permette di fornire applicativi di alta qualità, veloci ed ottimizzati per tutti i dispositivi in commercio." ) }}</p>
    </div>

    <div class="mb-4">
        <p class="mb-2 font-medium text-xl">{{ __( "La metodologia" ) }}</p>
        <p class="font-thin text-sm">{{ __( "Per ottenere buoni risultati è necessario avere un buon metodo di lavoro." ) }}</p>
        <p>{{ __( "Lo sviluppo di applicazioni negli ultimi dieci anni ha visto l'affermarsi di varie metodologie (Waterfall Model, Spiral Model) di lavoro sempre più raffinate e rispondenti alle esigenze del cliente (Scrum, Agile) in modo da colmare il grande divario che esisteva tra cliente e programmatore. In base al tipo di progetto da sviluppare ogni metodologia presenta, chiaramente, sia dei punti di forza che di debolezza." ) }}</p>
        <p class="mb-4">{{ __( "Ho sviluppato, nel corso degli anni, una metodologia di tipo incrementale in grado di dare al cliente la soddisfazione di crescere e veder crescere il proprio progetto senza togliere il piacere di utilizzare il prodotto già dalle prime fasi di sviluppo." ) }}</p>
        <p class="p-2 italic">{{ __( "Tutto questo diventa un sistema di analisi Agile accompagnato da uno sviluppo in miglioramento progressivo (Progressive Enhancement) che permette la produzione di Deliveries frequenti in grado di coinvolgere il cliente durante le fasi più critiche di lavorazione." ) }}</p>
    </div>

    <div class="mb-4">
        <p class="mb-2 font-medium text-xl">{{ __( "Il Framework" ) }}</p>
        <p class="font-thin text-sm">{{ __( "La struttura portante della tua applicazione" ) }}</p>
        <p class="mb-4">{{ __( "Ho sviluppato un framework proprietario modulare per la gestione della logica comune delle tue applicazioni basato sul famoso framework di ultima generazione Laravel." ) }}</p>
        <p class="p-2 italic">{{ __( "Lavoro quotidianamente per rafforzare le logiche di gestione ed aumentare la potenza della struttura, con lo scopo di fornirti un prodotto versatile, facile da utilizzare e sicuro." ) }}</p>
    </div>
</div>
