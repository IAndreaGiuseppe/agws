<div class="py-16 px-8">
    <p class="mb-2 font-medium text-xl uppercase">&#128225; {{ __( "Hosting" ) }}</p>

    <div class="mb-4">
        <p class="mb-2 font-medium text-xl">{{ __( "Il dominio" ) }}</p>
        <p class="font-thin text-sm">{{ __( "Tutto ciò di cui hai bisogno parte dal nome del tuo dominio" ) }}</p>
        <p class="mb-4">{{ __( "Valutiamo insieme le caratteristiche fondamentali del tuo progetto, verifichiamo la disponibilità in rete e ti forniamo una lista di domini tra cui scegliere." ) }}</p>
        <p class="p-2 italic">{{ __( "Per tutti i domini già registrati il traferimento verso i nostri piani di hosting è gratuito." ) }}</p>
    </div>

    <div>
        <p class="mb-2 font-medium text-xl">{{ __( "Rete" ) }}</p>
        <p class="font-thin text-sm">{{ __( "Connettività illimitata" ) }}</p>
        <p class="mb-4">{{ __( "Alla base di ogni sistema di comunicazione c'è una rete digitale, un'infrastruttura portante che connette tutti i dispositivi. Come per la tua rete aziendale, anche i dispositivi connessi a internet hanno bisogno di comunicare tra di loro." ) }}</p>
        <p class="p-2 italic">{{ __( "Fornisco un servizio scalabile in grado di adattarsi alle tue esigenze e di accompagnarti nella crescita della tua azienda." ) }}</p>
    </div>
</div>
