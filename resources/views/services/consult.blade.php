<div class="py-16 px-8 bg-agp-dark text-white">
    <p class="mb-2 font-medium text-xl uppercase">{{ __( "Consulenza" ) }}</p>

    <div>
        <p class="mb-2 font-medium text-xl">{{ __( "Chiedimi un consiglio" ) }}</p>
        <p class="font-thin text-sm">{{ __( "Riorganizza le idee" ) }}</p>
        <p>{{ __( "Se non hai ancora le idee chiare, contattami senza impegno anche solo per un consiglio." ) }}</p>

        <p class="py-2">
            <a href="tel:+393920460819" class="flex items-center text-agp-red">
                <span class="inline-block w-10 mr-2 txt-red-container">
                    @include('svg.phone')
                </span>

                {{ __( "Chiamami" ) }}
            </a>
        </p>
    </div>
</div>
