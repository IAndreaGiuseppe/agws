<div class="py-16 px-8 bg-agp-green text-agp-white">
    <div class="container mx-auto">
        <p class="mb-2 font-medium text-xl uppercase text-agp-yellow">{{ __( "Computer Skills" ) }}</p>

        <div>
            <p class="mb-2 font-medium text-xl">{{ __( "Linguaggi" ) }}</p>
            <p class="p-2 italic">{{ __( "Html5, Css3, PHP, Javascript (ECMA 2016), MySql, UML" ) }}</p>

            <p class="mb-2 font-medium text-xl">{{ __( "Frameworks" ) }}</p>
            <p class="p-2 italic">{{ __( "Symfony, Laravel, VueJS, Jquery, Tailwind, Bootstrap" ) }}</p>

            <p class="mb-2 font-medium text-xl">{{ __( "Software" ) }}</p>
            <p class="p-2 italic">{{ __( "Git, CLI, Putty, VS Code, Workbench, Firefox/Chrome, Thunderbird, Office, Adobe, Filezilla, Visual Paradigm" ) }}</p>

            <p class="mb-2 font-medium text-xl">{{ __( "Services" ) }}</p>
            <p class="p-2 italic">{{ __( "AWS, Github, Stackoverflow, Twitter, Google" ) }}</p>
        </div>
    </div>
</div>
