<div class="py-16 px-8 bg-agp-dark text-white text-center">
    <p class="mb-2 font-medium text-xl uppercase">{{ __( "Stai cercando uno sviluppatore web?" ) }}</p>

    <div>
        <p class="mb-2 font-medium text-xl">{{ __( "Chiedimi un consiglio" ) }}</p>
        <p class="font-thin text-sm">{{ __( "Riorganizza le idee" ) }}</p>

        <p class="py-2">
            <a href="tel:+393920460819" class="inline-flex items-center text-agp-red">
                <span class="inline-block w-10 mr-2 txt-red-container">
                    @include('agws::svg.phone')
                </span>

                {{ __( "Chiamami" ) }}
            </a>
        </p>
    </div>
</div>
